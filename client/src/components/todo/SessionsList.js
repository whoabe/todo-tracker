import React, { useState } from "react";

// const [open, setOpen] = useState(false);

const SessionsList = (todoId) => {
  //   setOpen(!open);
  return <h1>SesssionsList + {todoId}</h1>;
};

export default SessionsList;
